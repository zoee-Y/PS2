import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Button
} from "react-native";
import { ScrollView } from "react-native-gesture-handler";
import { firebase } from '../../firebase/config'

const TrDeliveries = ({ navigation }) => {

    return (
        <ScrollView>
            <Text>TrDeliveries Test</Text>
        </ScrollView>
    )

}

export default TrDeliveries;